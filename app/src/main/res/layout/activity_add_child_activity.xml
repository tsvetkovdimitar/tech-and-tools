<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddChildActivity">

    <TextView
        android:id="@+id/textViewChildName"
        android:textAlignment="center"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_width = "match_parent"
        android:layout_height = "wrap_content"
        android:inputType="text"
        android:id = "@+id/edt_activity_name"
        android:layout_below="@+id/textViewChildName"
        android:hint = "Activity type"
        android:layout_marginTop = "46dp" />


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:ems="10"
        android:id="@+id/edt_activity_note"
        android:layout_below="@+id/edt_activity_name"
        android:hint="Note" />

    <Button
        android:id="@+id/btn_add_activity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Add Activity"
        android:layout_below="@id/edt_activity_note"/>

    <TextView
        android:id="@+id/textViewActivity"
        android:textAlignment="center"
        android:layout_below="@id/btn_add_activity"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Activities"/>

    <ListView
        android:id="@+id/listviewActivities"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@id/textViewActivity">

    </ListView>

</RelativeLayout>